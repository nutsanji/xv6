1. Looking at the backtrace output, which function called syscall?
  answer: usertrap() 

2. What is the value of p->trapframe->a7 and what does that value represent?
   (Hint: look user/initcode.S, the first user program xv6 starts.)  
  answer: a7 = 0x7 (syscall number in syscall.h)

3.What was the previous mode that the CPU was in?
  answer: User-mode. (sstatus=0x22 the bit8(SPP)=0)

4. Write down the assembly instruction the kernel is panicing at. Which register corresponds to the varialable num?
 answer: lw a3, 0(zer0) #0x0 ;num=a3

5. Why does the kernel crash? Hint: look at figure 3-3 in the text; is address 0 mapped in the kernel address space? Is that confirmed by the value in scause above? (See description of scause in RISC-V privileged instructions)
  answer: the address we used in kernel is virtual address. and the address
  0 does not mapped into physical address.
  (the register scause=0xd which means: load page fault.)

6. What is the name of the binary that was running when the kernel paniced? What is its process id (pid)?
  answer: initcode; pid=1

